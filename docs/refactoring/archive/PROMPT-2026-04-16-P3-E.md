# Continuing P3: now implement P3-E

Pull before starting. P3-D is done (`5d0ccb8`).

**Before starting P3-E — check and fix a potential bug in `compare_binaries.sh`:**

The script sets `TIMEOUT=60` and passes `--timeout $TIMEOUT` to each binary. The solver's `--timeout` flag is documented in milliseconds, making this 60ms — far too short. The correct value for a 1-minute per-instance timeout should be `60000`. The harness still found matches during testing (likely because very early Klondike/FreeCell seeds solve trivially fast), but the timeout is not doing what was intended. Check the solver source or `--help` output to confirm the unit, then fix `scripts/compare_binaries.sh` if needed and commit the fixup before starting P3-E.

---

**Your task: Commit P3-E** — extend CTest with per-variant regression Level 1 targets. Read §Commits (P3-E) and §Success Criteria in `phase3_plan.md` before touching anything.

**Key context:**

- The pre-refactor baseline tag is **`pre-refactor-work`** (not whatever name you might expect from the plan docs).
- The three new CTest targets are `regression_level1_flat`, `regression_level1_lru`, and `regression_level1_hash_only`. Each runs the appropriate subset of Level 1 games — the plan specifies which games go in which target, including the `--force-lru` set for the LRU target and the allow-list requirement for hash-only.
- Read the existing `regression_level1` CTest definition in `CMakeLists.txt` carefully before writing anything — the new targets should follow the same pattern.

Scope for P3-E: `CMakeLists.txt` only (plus any script changes needed to pass the right binary path to the regression runner).
