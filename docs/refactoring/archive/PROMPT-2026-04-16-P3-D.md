# Continuing P3: now implement P3-D

Pull before starting. P3-C is done (`a62f126`).

Your task is **Commit P3-D**: the `compare_binaries.sh` validation harness. The script and the CTest wrapper target are both specified verbatim in §Validation Harness and §Commits of `phase3_plan.md` — follow them closely.

Two things to confirm before writing: (1) check where the binaries actually land after the P3-B CMake changes (`bin/` subdirectory or build root?) so the paths in the script are correct; (2) confirm `jq` is available or make the script fail gracefully if it isn't.

Scope: new file `scripts/compare_binaries.sh` and a CTest target addition in `CMakeLists.txt`.
