# Session Kickoff: Phase 2 Commit P2-A

Start a new session with this prompt.

---

You are picking up work on the ReSolvitaire caching refactor, Phase 2 (template cache unification). Phase 1 has landed on `dev`. The working branch `feature/template-cache` was just created from `dev` and is the branch you should be on.

**First, read these in order — they are the ground truth for this session:**

1. `docs/refactoring/PICKUP-phase2.md` — Phase 2 pickup. The "Next Session: Commit P2-A" section is what this session implements.
2. `docs/refactoring/phase2_plan.md` — the full Phase 2 plan. Read the §"Design" section (Policy concept, three concrete policies, memory layout assertions) carefully; P2-A depends on getting these right first time.
3. `docs/refactoring/phase2_3_workflow.md` — process rules, session cadence, merge gates. In particular: one commit per session, no scope creep, and KI-7 accordion failures are expected — do not investigate.

**Then read the three reference cache implementations** (you are reproducing their behaviour inside a single template):

- `src/main/game/flat_cache.h` and `flat_cache.cpp` — the TwoBig1 depth-preferred replacement policy
- `src/main/game/hash_only_cache.h` and `hash_only_cache.cpp` — simpler no-depth TwoBig1 with 0→1 hash normalisation
- `src/main/game/predecessor_flat_cache.h` and `predecessor_flat_cache.cpp` — the 128-byte cluster variant with the slot-1 hash guard optimisation

**Your task this session: Commit P2-A.**

Scope (strictly additive — do NOT modify any existing file):

- Create `src/main/game/generic_flat_cache.h` — the template class.
- Create `src/main/game/generic_flat_cache_policies.h` — the three policies (`CompactStatePolicy`, `HashOnlyPolicy`, `PredecessorPolicy`).
- Include three mandatory compile-time assertions on cluster sizes:
  - `sizeof(generic_flat_cache<CompactStatePolicy>::cluster) == 64`
  - `sizeof(generic_flat_cache<HashOnlyPolicy>::cluster)     == 16`
  - `sizeof(generic_flat_cache<PredecessorPolicy>::cluster)  == 128`
  - plus `alignof` matching on the first and third.
- The template must reproduce each original's behaviour exactly when instantiated with the matching policy. The hash-guard optimisation in `PredecessorPolicy` is the subtlest requirement and cannot be silently lost.
- C++14 only — **no `if constexpr`**. Use tag dispatch or SFINAE for the `HAS_HASH_GUARD` branching.
- Nothing in the existing codebase should call the new template yet. `cache_factory.h` wiring is a later commit (P2-C); do not touch it.
- Do NOT modify `flat_cache.*`, `hash_only_cache.*`, `predecessor_flat_cache.*`, `cache_factory.h`, or anything under `src/main/game/search-state/`.

**Build verification for P2-A:**

```bash
# Default build must remain unaffected
./build.sh --release --unit-tests

# Template-enabled build must compile (even though nothing calls it yet)
cd cmake-build-release && cmake -DUSE_GENERIC_CACHE=ON .. && make -j4
```

Wait — the `USE_GENERIC_CACHE` CMake option is added in P2-C, not P2-A. For P2-A, it is enough that the new headers are well-formed and get included somewhere that proves they compile (even if just a throwaway `#include` in one cpp file — but discuss with Ian before adding any #include outside the new files). Practically, the safest P2-A check is: the new headers compile standalone when directly included from a test or from a one-line compile-check translation unit. If no existing file includes them, their static_asserts don't fire. You may need to add a minimal compile-test (e.g. a new empty `.cpp` in `src/test/unit_tests/` that just `#include`s the two new headers) to force instantiation and trigger the asserts. Raise this with Ian at the start of the session before writing code.

**Process rules — non-negotiable:**

1. Bug encountered → STOP, report to Ian in one paragraph, ask how to proceed. Do not investigate. Do not attempt a fix.
2. Semantic question → STOP, ask Ian. He is the domain expert.
3. Scope → exactly this one commit. Do not drift into P2-B or later.
4. Test failure → a bug report to Ian, not a debugging task.
5. KI-3 `BlackHoleUsesNewCache` and KI-7 `AccordionAgreement` are pre-existing failures — ignore them. Any *new* failure is a blocker.
6. Risky git operations (merges, pushes, force-pushes, branch deletes) require fresh explicit permission from Ian each time — the previous session's blanket for the Phase 1 landing does not carry over.

**End-of-session protocol:**

1. Run the validation checklist for P2-A from `PICKUP-phase2.md`.
2. If green: create the commit, update `PICKUP-phase2.md` to mark P2-A as DONE (with the git hash), update "Next Session" to point at P2-B, show Ian the PICKUP diff for confirmation before ending.
3. If red: stop, write the symptom into `PICKUP-phase2.md` under "Current Blocker," ask Ian.

**Do not touch anything outside the P2-A scope.** When in doubt, ask Ian first.
