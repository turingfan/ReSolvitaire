# Continuing P3: now implement P3-C

Pull before starting. P3-B is done (`b0e418f`). The factory already throws `std::runtime_error` for ineligible flag+game combinations in all three variant branches — that part of P3-C is already complete.

Your task is the `main.cpp` side: catch those exceptions and emit a clean error message to stderr (exit code 1) instead of letting the process terminate with an unhandled exception. Read the P3-C entry in `PICKUP-phase3.md` and the §Commits section of `phase3_plan.md` for the exact cases that must produce clean output rather than a crash.

Scope: `src/main/main.cpp` only. The `cache_factory.h` changes listed in the plan for P3-C are already done.
