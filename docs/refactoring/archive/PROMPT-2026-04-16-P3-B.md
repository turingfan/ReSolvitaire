# Continuing P3: now implement P3-B

Pull the branch before starting — a fixup commit (`d09dd43`) was added after your P3-A: the `old_*` variable declarations in the four Zobrist update functions were moved inside their `#if SOLVITAIRE_COMPUTES_FLAT_HASH` guards to prevent `-Wunused-variable -Werror` failures when building the LRU-only target. No action needed beyond pulling; just be aware the guard structure is now:

```cpp
#if SOLVITAIRE_COMPUTES_FLAT_HASH
    uint8_t old_desc = payload.get_descriptor(cid);
    zobrist_hash_value ^= ...;
#endif
```

Your next task is **Commit P3-B**: `CMakeLists.txt` + `cache_factory.h`. Read the P3-B entry in `PICKUP-phase3.md` and the §CMake Target Structure and §LRU Binary Semantics sections of `phase3_plan.md` before writing anything. Include the `nm` Zobrist-strip verification in your build checks.
